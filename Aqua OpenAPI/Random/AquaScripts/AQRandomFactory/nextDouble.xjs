print("Generate a random double:");
for ( i = 0; i < 5; i++ ) {
    var x = aqua.random.nextDouble();
    print("\t"+x);
}

var min = -1000;
var max = 1000;
print("Generate a random double between " + min + " and " + max + ":");
for ( i = 0; i < 5; i++ ) {
    var x = aqua.random.nextDouble(min, max);
    print("\t"+x);
}

