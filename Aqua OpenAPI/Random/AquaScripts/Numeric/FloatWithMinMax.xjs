importPackage(java.lang);

print("Generate random float numeric values with Min & Max values:");
min = new Float("1");
max = new Float("150");
for ( i = 0; i < 5; i++ ) {
    var x = aqua.random.nextFloat(min, max);
    print("\t"+x);
}
