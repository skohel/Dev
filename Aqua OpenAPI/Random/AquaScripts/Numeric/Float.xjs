// Returns the next pseudorandom, uniformly distributed float value between 0.0 and 1.0 from this random number generator's sequence.
print("Generate a random Float value:");
for ( i = 0; i < 5; i++ ) {
    var x = aqua.random.nextFloat();
    print("\t"+x);
}
