var factory = aqua.type.getDate();

// get current datetime
var date1   = factory.newDate();
aqua.util.sleep(15);
var date2   = factory.newDate();

aqua.console.println("Is [" + date2 + "] later then [" + date1 + "]? " + factory.isLater(date2, date1));

aqua.console.println("Is [" + date1 + "] later then [" + date2 + "]? " + factory.isLater(date1, date2));
