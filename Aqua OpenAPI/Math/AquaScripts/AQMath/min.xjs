/*
aqua.math.min

Returns the smaller of two double values.  If the arguments have the same value, the result is that same value. 
   
Synopsys
    double aqua.math.min(double value1, double value2)

Description
    Nothing exciting to describe.

Example
    see below
*/

// --- min() ---
function show_min(v1, v2) {
    aqua.console.println("min[" + v1 + ", " + v2 + "] --> [" + aqua.math.min(v1, v2) + "]");
}

show_min(2, 7);
show_min(880, 280);
show_min(2.345, 9.876);
show_min(9.876, 9.875);