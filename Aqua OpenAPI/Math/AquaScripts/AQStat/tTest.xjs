

var rawData=new Array();
var out=0;

//generate data
for(i=0;i<30;i++){

   rawData[i]=10+2*Math.random()-1;
   
}

out=aqua.math.stat.tTest(rawData);

aqua.console.println("t value, for zero mean = " + out);


out=aqua.math.stat.tTest(rawData,10);

aqua.console.println("t value, for mean of 10 = " + out);

