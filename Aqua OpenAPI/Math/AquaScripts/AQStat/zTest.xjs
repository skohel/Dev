

var rawData=new Array();
var out=0;
var mu=10;
var sigma=1;

//generate data
for(i=0;i<30;i++){

   rawData[i]=10+2*Math.random()-1;
   
}

out=aqua.math.stat.ztest(rawData,mu ,sigma);

aqua.console.println("z Test = " + out);

